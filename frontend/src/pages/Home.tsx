import React, { FC } from 'react';
import { MintContainer, Hero } from '../components';

export const Home: FC = () => {
  return (
    <div>
      <Hero />
      <MintContainer />
    </div>
  );
};
